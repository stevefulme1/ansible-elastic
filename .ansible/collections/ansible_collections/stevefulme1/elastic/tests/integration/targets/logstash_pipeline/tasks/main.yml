---
# Integration tests for stevefulme1.elastic.logstash_pipeline
# These tests verify full CRUD lifecycle with idempotency and check_mode.
# A live API endpoint is required.
- name: Set test variables
  ansible.builtin.set_fact:
    test_logstash_pipeline_params:
      description: "test_description_value"
      last_modified: "test_last_modified_value"
      pipeline: "test_pipeline_value"
      pipeline_metadata: "test_pipeline_metadata_value"
      pipeline_settings: "test_pipeline_settings_value"
      username: "test_username_value"
# ============================================================
# CREATE - check mode
# ============================================================
- name: Create logstash_pipeline (check mode)  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    state: present
  args: "{{ test_logstash_pipeline_params }}"
  check_mode: true
  register: create_check
- name: Assert create in check mode reports changed
  ansible.builtin.assert:
    that:
      - create_check is changed
    fail_msg: "Create in check mode should report changed=true"
# ============================================================
# CREATE - real
# ============================================================
- name: Create logstash_pipeline  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    state: present
  args: "{{ test_logstash_pipeline_params }}"
  register: create_result
- name: Assert create reports changed
  ansible.builtin.assert:
    that:
      - create_result is changed
    fail_msg: "Create should report changed=true"
- name: Record created resource identifier
  ansible.builtin.set_fact:
    created_id: "{{ create_result.id }}"
# ============================================================
# CREATE - idempotency
# ============================================================
- name: Create logstash_pipeline again (idempotency)  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    state: present
  args: "{{ test_logstash_pipeline_params }}"
  register: create_idempotent
- name: Assert create is idempotent
  ansible.builtin.assert:
    that:
      - create_idempotent is not changed
    fail_msg: "Repeated create with same params should report changed=false"
# ============================================================
# READ - verify created resource
# ============================================================
- name: Get logstash_pipeline info  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline_info:
    id: "{{ created_id }}"
  register: info_result
- name: Assert resource exists
  ansible.builtin.assert:
    that:
      - info_result.logstash_pipelines | length == 1
    fail_msg: "Created resource does not match expected values"
# ============================================================
# UPDATE
# ============================================================
# No optional string parameters available for update testing.
# ============================================================
# DELETE - check mode
# ============================================================
- name: Delete logstash_pipeline (check mode)  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    id: "{{ created_id }}"
    state: absent
  check_mode: true
  register: delete_check
- name: Assert delete in check mode reports changed
  ansible.builtin.assert:
    that:
      - delete_check is changed
    fail_msg: "Delete in check mode should report changed=true"
- name: Verify logstash_pipeline still exists after check mode delete  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline_info:
    id: "{{ created_id }}"
  register: delete_check_verify
- name: Assert resource still exists after check mode delete
  ansible.builtin.assert:
    that:
      - delete_check_verify.logstash_pipelines | length == 1
    fail_msg: "Resource should still exist after check mode delete"
# ============================================================
# DELETE - real
# ============================================================
- name: Delete logstash_pipeline  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    id: "{{ created_id }}"
    state: absent
  register: delete_result
- name: Assert delete reports changed
  ansible.builtin.assert:
    that:
      - delete_result is changed
    fail_msg: "Delete should report changed=true"
# DELETE - idempotency
- name: Delete logstash_pipeline again (idempotency)  # noqa: args[module]
  stevefulme1.elastic.logstash_pipeline:
    id: "{{ created_id }}"
    state: absent
  register: delete_idempotent
- name: Assert delete is idempotent
  ansible.builtin.assert:
    that:
      - delete_idempotent is not changed
    fail_msg: "Repeated delete should report changed=false"
