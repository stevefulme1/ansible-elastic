---
argument_specs:
  main:
    short_description: "Rolling upgrade of Elasticsearch cluster"
    options:
      elastic_upgrade_host:
        description: Target host address
        type: str
        required: true
      elastic_upgrade_username:
        description: Authentication username
        type: str
      elastic_upgrade_password:
        description: Authentication password
        type: str
        no_log: true
      elastic_upgrade_api_key:
        description: API key for authentication
        type: str
        no_log: true
      elastic_upgrade_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      elastic_upgrade_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
